# AirStream

Real-time flight tracking system using a Raspberry Pi Zero with an ADS-B receiver. It publishes live aircraft data to Redis, which is streamed via WebSockets to an interactive React map for geospatial visualization.
